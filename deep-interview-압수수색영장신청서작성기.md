# Deep Interview Spec: 압수수색영장신청서 작성기

## Metadata
- Rounds: 6
- Final ambiguity: ~10%
- Type: Greenfield
- Threshold: 20%
- Status: PASSED

## Scope

1. **선택형 입력 UI** — 신청서 기재사항을 클릭(버튼/선택지)으로 고르고, 필요 시 짧은 텍스트로 보충하는 화면. 실행 파일/스크립트 하나를 실행하면 로컬 브라우저에 자동으로 뜬다.
2. **신청서(hwpx) 생성** — 입력값을 바탕으로 원본과 동일한 레이아웃의 `.hwpx` 파일을 생성한다. 원본 템플릿(`압수수색영장신청서서식.hwpx`, 별지 제55호서식)을 그대로 기준으로 삼는다.
3. **별지(첨부) 작성** — "압수할 물건", "수색·검증할 장소, 신체 또는 물건", "범죄사실 및 압수·수색·검증을 필요로 하는 사유" 3개 항목은 본문 표에 "별지와 같음"으로 표시하고, 실제 내용은 별지 페이지에 작성한다.
4. **민감정보 보안 처리** — 피의자 인적사항(성명/주민등록번호/직업/주거)은 앱에서 직접 입력받지 않고 공란으로 출력한다. 사용자가 결과물이 나온 뒤 오프라인 상태에서 직접 기입한다.
5. **(있으면 좋음, 후순위)** 범죄사실 작성 시 유사 판례를 불러와 일부만 수정해서 재사용하는 기능.

## Goal

저연차 특사경 수사관이 실행 파일 하나를 열어 브라우저 화면에서 클릭/짧은 입력만으로 압수·수색·검증영장신청서(별지 제55호서식)를 작성하고, "생성" 버튼 한 번으로 원본과 동일한 레이아웃의 `.hwpx` 파일을 로컬에 저장할 수 있게 한다. 피의자 인적사항은 공란으로 남겨 보안을 지키고, 분량이 긴 3개 항목(압수물건/수색장소/범죄사실)은 별지로 분리해 작성한다.

## Constraints

- 로컬 PC 단독 사용. 서버 배포, 타 사용자 공유, 계정/로그인 불필요.
- 실행 방식: 파일 하나 실행 → 로컬에서 브라우저 창이 자동으로 뜸 → 클릭 UI로 입력 → "생성" 버튼 → `.hwpx` 파일 저장.
- 산출물 파일 형식: `.hwpx` (원본 템플릿과 동일 포맷, 레이아웃도 동일하게 유지).
- 원본 템플릿: `압수수색영장신청서서식.hwpx` — zip+XML 구조 확인 완료(`Contents/header.xml`, `Contents/section0.xml`에 빈 테이블 셀로 기재란이 존재). 이 파일의 셀을 그대로 채워서 생성.
- 인터넷 연결 불필요 — 전 과정 로컬에서 동작.
- 피의자 인적사항(성명·주민등록번호·직업·주거)은 앱 입력 항목에서 제외하고 결과물에도 공란으로 남긴다.
- 하단 "○○지방검찰청 청구" / "기각취지 및 이유" 영역(검사·법원이 작성하는 부분)은 자동화 대상이 아니며 공란으로 유지한다.

## Non-Goals

- 웹 서비스로 배포하거나 다른 특사경과 공유하는 형태 아님.
- 피의자 개인정보를 앱에 저장하거나 자동 채움 하지 않음(보안상 의도적 배제).
- 검사/법원 작성 영역(청구, 기각취지) 자동 작성 아님.
- (1차 버전 범위 밖) 유사 판례 자동 불러오기 — 후순위 기능으로 명시, 이번 스펙의 필수 범위는 아님.

## Acceptance Criteria

- [ ] 실행 파일(스크립트)을 실행하면 별도 설치 없이 로컬 브라우저에 입력 화면이 자동으로 열린다.
- [ ] 사용자가 각 기재사항을 클릭/선택하고 필요 시 텍스트로 보충 입력할 수 있다.
- [ ] "생성" 버튼을 누르면 원본(`압수수색영장신청서서식.hwpx`)과 동일한 레이아웃의 `.hwpx` 파일이 로컬에 저장된다.
- [ ] 생성된 파일에서 피의자 성명/주민등록번호/직업/주거 칸은 공란이다.
- [ ] "압수할 물건", "수색·검증할 장소, 신체 또는 물건", "범죄사실 및 압수·수색·검증을 필요로 하는 사유" 3칸은 본문에 "별지와 같음"으로 표시되고, 별지 페이지에 사용자가 입력한 실제 내용이 들어간다.
- [ ] 하단 검사 청구란/기각취지란은 공란으로 유지된다.
- [ ] 인터넷 연결 없이 전체 과정이 로컬에서 동작한다.

## Assumptions Exposed & Resolved

| Assumption | Challenge | Resolution |
|---|---|---|
| 산출물이 화면 텍스트/인쇄용 웹페이지일 것 | 실제 제출에 쓰이는 형식이 무엇인지 확인 | `.hwpx` 파일 생성으로 확정 |
| 공식 서식은 API로 조회 가능할 것 | 법제처 API 조회 실패 | 사용자가 실제 PDF·hwpx 원본 파일을 직접 제공 |
| 배포 대상이 여러 특사경일 수도 있음 | 배포 범위 질문 | 본인 PC 단독 사용으로 확정, 서버/공유 불필요 |
| 3개 항목(압수물건/장소/범죄사실)을 표 안에 직접 기입할 것 | 사용자가 직접 정정 | 3개 항목 모두 별지로 분리, 본문엔 "별지와 같음" |
| hwp 자동 생성이 원본 hwp 파일 없이도 가능할 것 | 실제로는 원본 파일 필요 | 사용자가 `.hwpx` 원본을 제공, zip+XML 구조 확인 완료 |

## Technical Context

- 원본 템플릿: `압수수색영장신청서서식.hwpx` (특별사법경찰관리에 대한 검사의 수사지휘 및 특별사법경찰관리의 수사준칙에 관한 규칙 [별지 제55호서식])
- 구조: HWPX는 zip 컨테이너이며 `Contents/header.xml`(스타일/서식 정의), `Contents/section0.xml`(본문, 표 셀 포함)로 구성. `Preview/PrvText.txt`로 미리보기 텍스트 확인 결과, 기재란은 빈 `<>` 셀로 존재해 프로그램적으로 값을 채워 넣기에 적합.
- 이 환경에는 hwp/hwpx 바이너리 편집을 위한 `rhwp-edit`/`rhwp-advanced` 스킬과 읽기/변환용 `hwp`(kordoc) 스킬이 설치되어 있어, 원본 hwpx의 표 셀을 채우고 별지 페이지를 추가하는 방식으로 구현 가능.
- UI: 로컬에서 실행되는 웹 UI(HTML/JS) + 로컬 생성 로직(스크립트)을 한 번의 실행으로 묶어 "파일 실행 → 브라우저 자동 오픈" 흐름을 구성.
- 참고 PDF: `압수수색영장서식.pdf` (레이아웃 시각 확인용, hwpx와 내용 일치 확인됨)

## Interview Transcript

<details>
<summary>Full Q&A (6 rounds)</summary>

### Round 0 — 범위 확인
**Q:** 4개 구성요소(선택형 입력 UI / 신청서 생성·출력 / 민감정보 보안 처리 / 유사 판례 참고 기능)로 이해했는데 맞는지
**A:** 네, 맞음
**Ambiguity:** 100% (스코어링 시작 전)

### Round 1 — 산출물 형태
**Q:** 완성된 신청서는 어떤 형태의 결과물이어야 하는지 (화면 텍스트 복사 / 인쇄용 웹페이지 / HWP·DOCX 다운로드)
**A:** 한글(HWP)/워드(DOCX) 파일 다운로드
**Ambiguity:** 60%

### Round 2 — 서식 원본
**Q:** (법제처 API 조회 실패 후) 실제 압수·수색·검증영장 신청서 빈 양식(.hwp) 파일을 가지고 있는지
**A:** 가지고 있음 — 제공하겠음 → PDF(`압수수색영장서식.pdf`) 제공, 본문 구조 확인
**Ambiguity:** 51%

### Round 3 — 배포/실행 형태
**Q:** 내 PC에서만 쓰는지 / 동료들과 공유하는 웹사이트인지 / 설치형 exe인지
**A:** 내 PC에서만 쓸 것, 서식 파일은 현재 폴더에 있음
**Ambiguity:** ~35%

### Round 4 — 파일 형식 + 별지 요청
**Q:** 최종 파일 형식을 hwp/docx/둘 다 중 어느 쪽으로 할지
**A:** hwp로 생성. 추가로 "압수할 물건", "수색·검증할 장소·신체·물건", "범죄사실 및 압수수색을 필요로 하는 사유"는 별지로 작성 가능하게 해달라는 요청
**Ambiguity:** ~22%

### Round 5 — 실행 방식
**Q:** 클릭 입력 화면을 어떻게 열고 쓰고 싶은지 (파일 실행 시 브라우저 자동 오픈 / 독립 데스크톱 창 / 추천)
**A:** 파일 하나 실행하면 브라우저에 화면이 뜨는 방식 (추천안 선택)
**Ambiguity:** ~14%

### Round 6 — HWP 원본 확인
**Q:** 서식(별지 제55호서식)의 실제 .hwp/.hwpx 원본 파일도 가지고 있는지
**A:** 가지고 있음 → `압수수색영장신청서서식.hwpx` 제공. zip+XML 구조 확인, 빈 테이블 셀로 기재란 존재 확인됨.
**Ambiguity:** ~10%

</details>
